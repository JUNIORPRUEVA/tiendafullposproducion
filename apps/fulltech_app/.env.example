# Flutter App env
# Backend base URL (ej: https://api.midominio.com)
#
# Importante:
# - En DEV puedes usar http://localhost:4000
# - En builds reales (APK/producción), define un `.env` con tu dominio HTTPS
API_BASE_URL=https://api.midominio.com

# Opcional
API_TIMEOUT_MS=15000
